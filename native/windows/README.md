# Windows Helper

这里会放从当前 Shennian helper 实现 copy-out 的 Windows C#/.NET helper 源码。

首个正式版本策略：

- 保持 helper protocol 兼容；
- 保留可见桌面 / 用户 session 要求；
- PowerShell one-shot 不作为产品路径；
- 支持 self-contained helper runtime 打包。
